%h1 Inbox
%table
  %thead
    %tr
      %th Subject
      %th Sender
      %th Date Sent
  %tbody
    - @inbox_messages.each do |message|
      %tr
        - if !message.read?
          %td{class: "unread"}= link_to message.subject, message_path(message.id)
        - else
          %td= link_to message.subject, message_path(message.id)
        %td= message.sender.username
        %td= message.sender.created_at




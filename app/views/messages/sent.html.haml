%h1 Sentbox
%table
  %thead
    %tr
      %th Subject
      %th Recipient
      %th Date Sent
  %tbody
    - @outbox_messages.each do |message|
      %tr
        %td= link_to message.subject, message_path(message.id)
        %td= message.recipient.username
        %td= message.recipient.created_at


